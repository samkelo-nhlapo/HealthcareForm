-- V4__seed_lookups.sql
-- Seed lookup/reference data (countries, provinces, cities, genders, marital status, allergies, medications)
-- Splitting seed data into focused files helps rerunnable migrations and easier reviews.

-- Example: countries insert
-- INSERT INTO Location.Countries (...) VALUES (...);
PRINT 'V4 no-op: lookup seed data is already included in V1 baseline.';
